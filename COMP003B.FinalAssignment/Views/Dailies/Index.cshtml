@model IEnumerable<COMP003B.FinalAssignment.Models.Daily>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MealTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatorId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecipeId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MealTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatorId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RecipeId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DailyId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DailyId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DailyId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
